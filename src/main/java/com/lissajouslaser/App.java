package com.lissajouslaser;

/**
 * Class for starting application.
 */
public final class App {
    private App() {
    }

    /**
     * Instantiates Loop and starts repl.
     */
    public static void main(String[] args) throws SyntaxException, ArityException {
        // Loop repl = new Loop();
        // repl.start();

        // TokensList item1 = new TokensList();
        // item1.add(new Token("aha"));
        // item1.add(new TokensList());
        // TokensList item2 = new TokensList();
        // item2.add(new Token("aha"));
        // item2.add(new TokensList());

        // item1.add(new Token("month"));
        // System.out.println(item1.equals(item2));

        // System.out.println(item1);
    }
}
